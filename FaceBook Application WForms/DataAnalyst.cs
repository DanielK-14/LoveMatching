using FacebookWrapper.ObjectModel;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;

namespace FaceBook_Application_WForms
{
    internal static class DataAnalyst
    {/*
        public static List<string> AnalyzeData(object obj)
        {
            List<string> analayzedData = new List<string>();
            StringBuilder stringBuilder;
            if(obj is Post)
            {
                analyzePost(obj as Post);
            }
            else if(obj is User)
            {
                analyzeFriend(obj as User);
            }
            else
            {
                analyzeEvent(obj as Event);
            }
        }

        private static void analyzeEvent(Event @event)
        {
            throw new NotImplementedException();
        }

        private static void analyzeFriend(User user)
        {
            throw new NotImplementedException();
        }

        private static void analyzePost(Post post)
        {
            throw new NotImplementedException();
        }
    }
}
*/
    }
}